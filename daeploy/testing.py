from unittest.mock import patch  # noqa F401, pylint: disable=unused-import

# We realized it's easier to just use the tools available than to write
# our own mocking functions, because it's hard to make the mocks work
# across namespaces.
